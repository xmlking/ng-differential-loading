# Playbook

```bash
ng new ngx-starter-kit --create-application=false
cd ngx-starter-kit
ng generate application webapp
ng generate library mylib

ng g component home --project=webapp -d
ng g component PageNotFound  --project=webapp -d
```

